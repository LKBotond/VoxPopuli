# User service
This microservice handles CRUD operations for users. It is responsible for fetching and storing user data, while authentication and authorization are managed separately by the Authentication Service.
